// Write a C program to sort a string array in ascending order.
#include<stdio.h>
#include<conio.h>
#include<string.h>
int main()
{
    char str[10];
    int i;
    printf("enter the string:");
    for(i=0;i<=9;i++)
        scanf("%c",&str[i]);

        printf("%s ",str);
    return 0;
}
